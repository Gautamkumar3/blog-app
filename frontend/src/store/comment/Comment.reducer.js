const initialState = {
    comment: [],
    loading: false,
    error: false
}

export const commentReducer = (state = initialState, { type, payload }) => {
    switch (type) {
        default: {
            return state;
        }
    }
}